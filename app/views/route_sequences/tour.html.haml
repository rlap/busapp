/ All of this is inside #tour div
%h1 Current Tour

%ul
  - @route_sequences.each do |stop|
    %li 
      = stop.stop_name

#lightbox
  #popup-title
    %a{href: "/"}
      %span.glyphicon.glyphicon-chevron-left#audio-back-button
    %h1 This is the title bar
  #popup-img
    %h1 This is the pop up image
  #popup-audio
    %audio{controls: "controls"}
      %source{src: "/lib/assets/the_beatles.mp3", type: "audio/mpeg"}
        Your browser does not support the audio element. 
